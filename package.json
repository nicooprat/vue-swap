{
  "name": "vue-swap-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/vue-swap"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
